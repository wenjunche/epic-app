<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OIDC Demo Home (TypeScript)</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; line-height: 1.6; padding: 20px; background-color: #f4f7f6; color: #333; }
        .container { max-width: 600px; margin: 40px auto; padding: 30px; background-color: #fff; border-radius: 8px; box-shadow: 0 4px 10px rgba(0,0,0,0.05); text-align: center; }
        h1 { color: #2c3e50; }
        p { font-size: 1.1em; }
        .button { display: inline-block; padding: 12px 24px; margin: 10px; font-size: 1em; text-decoration: none; color: #fff; border-radius: 5px; transition: background-color 0.3s; }
        .login { background-color: #3498db; }
        .login:hover { background-color: #2980b9; }
        .logout { background-color: #e74c3c; }
        .logout:hover { background-color: #c0392b; }
        .profile { background-color: #2ecc71; }
        .profile:hover { background-color: #27ae60; }
    </style>
</head>
<body>
<div class="container">
    <h1>Welcome to the OIDC Demo App (TS)</h1>
    <% if (user) { %>
        <p>Hello, <strong><%= user.displayName || user.username || 'User' %></strong>!</p>
        <p>You are logged in.</p>
        <a href="/profile" class="button profile">View Profile</a>
        <a href="/logout" class="button logout">Log Out</a>
    <% } else { %>
        <p>You are not logged in.</p>
        <a href="/login" class="button login">Log In with OIDC</a>
    <% } %>
</div>
</body>
</html>
